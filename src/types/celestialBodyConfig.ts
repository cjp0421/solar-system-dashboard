import type { ReactNode } from "react";

export type CelstialBodyConfig = {
    title: string;
    subtitle: string;
    ctaLabel: string;
    ctaTarget: string;
    ctaAnimation?: ReactNode;
    factsTitle: string;
}