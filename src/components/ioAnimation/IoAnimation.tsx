import { Box } from "@mui/material";
import "./IoAnimation.css";

export default function IoAnimation() {
    return (
        <Box
            className="io-background"
            sx={{
                margin: "0 auto",
                position: "relative",
            }}
        />
    );
}