import { Box } from "@mui/material";
import "./PhobosAnimation.css";

export default function PhobosAnimation() {
    return (
        <Box
            className="phobos-background"
            sx={{
                margin: "0 auto",
                position: "relative",
            }}
        />
    );
}